apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-authservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-authservice
  template:
    metadata:
      labels:
        app: postgres-authservice
    spec:
      priorityClassName: high-priority
      containers:
        - name: postgres
          image: 'postgres:15'
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: auth-service-config
                  key: DB_NAME
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: auth-service-secret
                  key: DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-service-secret
                  key: DB_PASSWORD
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: postgres-data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: postgres-auth-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-authservice
spec:
  ports:
    - port: 5434
      targetPort: 5432
  selector:
    app: postgres-authservice
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-auth-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi